---
layout: base.njk
---

<div class="main-layout">
    <section class="news-column">
        <div class="section-title">TAG: {{ tagDisplay | upper }}</div>

        <p class="back-link"><a href="/tags/">← Back to all tags</a></p>

        {% set taggedArticles = collections.articles | filterByTag(tag) %}
        {% for article in taggedArticles %}
        <article class="news-item{% if loop.first %} featured{% endif %}">
            <header class="news-header">
                {% if article.data.label %}
                <span class="news-label {{ article.data.label | lower }}">{{ article.data.label }}</span>
                {% endif %}
                <h2><a href="{{ article.url }}">{{ article.data.title }}</a></h2>
                {% if article.data.hot %}
                <span class="hot-badge">HOT!</span>
                {% endif %}
            </header>
            <div class="news-body">
                {{ article.content | excerpt | safe }}
            </div>
        </article>
        {% if loop.index == 2 %}
        <div class="hr-fancy" role="separator"></div>
        {% endif %}
        {% else %}
        <article class="news-item">
            <header class="news-header">
                <span class="news-label sources">SOURCES</span>
                <h2>No Articles Yet</h2>
            </header>
            <div class="news-body">
                No articles with this tag yet.
            </div>
        </article>
        {% endfor %}
    </section>

    {% include "partials/sidebar.njk" %}
</div>
